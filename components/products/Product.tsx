import { Product } from "@/types";

interface Props {
    article: Product
}

const ShowProduct = ({article}: Props) => {
    return (
        <li>

        </li>
      );
}
 
export default ShowProduct;